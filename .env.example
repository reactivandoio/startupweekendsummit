# Pasta onde as inscrições são salvas (data/volunteers.json).
# Docker dev: /app/data (montado do host em ./data). Docker prod: /data (volume nomeado "data").
# Fora do Docker o padrão é ./data
# DATA_DIR=
